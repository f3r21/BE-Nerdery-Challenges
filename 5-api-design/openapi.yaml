# Skeleton only. The contract itself (paths, schemas, status codes, error shape) is
# authored by hand against DECISIONS.md. What is here exists to prove the toolchain runs
# before the document is two thousand lines long:
#
#   npx --yes @redocly/cli@latest lint 5-api-design/openapi.yaml
#   npx --yes @stoplight/spectral-cli@6 lint 5-api-design/openapi.yaml
#
# Both are clean at 0 errors. Three warnings are left standing on purpose rather than
# silenced with placeholder data: `info-license` and `info-contact` (this is coursework,
# not a published API) and `no-server-example.com`. The server really is localhost. That
# rule exists for public descriptions.
#
# 3.0.3, not 3.1: @nestjs/swagger emits 3.0.0, so Week 3's generated document stays
# diffable against this one. The concrete tell is nullability: 3.0 spells it
# `nullable: true`, 3.1 spells it `type: [string, "null"]`.
openapi: 3.0.3

info:
  title: T-Shirt Store API
  version: 0.1.0
  description: |
    Contract for the T-Shirt Store API. Week 3 implements this document. If the
    implementation and the document disagree, the document is not out of date.

    Written against ERD commit `8d47aae` on `challenge/erd`.

    **Money.** Every amount is an integer in minor units. 1999 means 19.99. The store
    is single-currency and prices are in USD. No response carries a currency field.

    **Tokens.** An access token is valid for 15 minutes. A refresh token is valid for
    7 days and rotates on every use. Sign-out deletes the presented device's refresh
    row. It does not revoke an access token that is already issued, so revocation lags
    by up to 15 minutes. Changing or resetting a password deletes every refresh row for
    that user.

servers:
  - url: http://localhost:3000/v1
    description: Local development. The /v1 prefix lives here, not on each path key.

tags:
  - name: auth
    description: Sign up, sign in, sign out, token refresh, password reset.

security:
  - bearerAuth: []

paths:
  # Path keys carry NO version prefix. The /v1 lives in servers.url, so writing
  # `/v1/products` here produces the URL /v1/v1/products. DECISIONS.md item 1.
  #
  # TODO: the operation below is a placeholder. It exists to keep the document valid and
  # to prove the linters and editor.swagger.io accept it. Its request and response shapes
  # predate the components layer. Replace it when the auth block is authored.
  /auth/sign-in:
    post:
      tags: [auth]
      summary: Exchange credentials for an access token
      description: |
        This operation is public. It does not need a token.

        Send the email and the password. The server returns an access token.

        This operation returns an access token only. A later commit adds the refresh
        token to this response, and adds `POST /auth/refresh`. DECISIONS.md item 8 sets
        both.
      operationId: signIn
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                  format: password
      responses:
        '200':
          description: Signed in
          content:
            application/json:
              schema:
                type: object
                properties:
                  accessToken:
                    type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

components:

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:

    # ---- Errors, DECISIONS.md item 2 ----------------------------------------
    # RFC 7807. Served as application/problem+json, never application/json.
    Problem:
      type: object
      required: [type, title, status]
      properties:
        type:
          type: string
          format: uri
          description: Identifies the problem kind. Two requests that failed for the
            same reason carry the same type.
          example: https://tshirt.store/problems/email-taken
        title:
          type: string
          description: A short summary of the problem kind. It does not change between
            occurrences.
          example: Email already registered
        status:
          type: integer
          description: The HTTP status code. It repeats the status line.
          example: 409
        detail:
          type: string
          description: What went wrong in this specific occurrence.
          example: An account with this email already exists.
        instance:
          type: string
          format: uri
          description: Identifies this occurrence.
        errors:
          type: array
          description: Extension member. Present only on a 400 from request validation.
            One entry per rejected field.
          items:
            type: object
            required: [field, message]
            properties:
              field:
                type: string
                example: email
              message:
                type: string
                example: must be a valid email address

    # ---- Pagination, DECISIONS.md item 3 ------------------------------------
    # Every collection returns this envelope. No endpoint returns a bare array.
    PageMeta:
      type: object
      required: [total, limit, offset]
      properties:
        total:
          type: integer
          description: Rows matching the filter, before limit and offset apply.
          example: 347
        limit:
          type: integer
          description: Rows in this page. The default is 20 and the maximum is 100.
          default: 20
          maximum: 100
          example: 20
        offset:
          type: integer
          description: Rows skipped. Offset pagination drifts if rows are inserted
            during a scan.
          default: 0
          example: 40

    # ---- Money, DECISIONS.md item 6 -----------------------------------------
    Money:
      type: integer
      description: An amount in minor units. 1999 means 19.99. The store is
        single-currency and prices are in USD.
      example: 1999

    # ---- Enums --------------------------------------------------------------
    # `delivered` and DELIVERY_PERSON belong to the Optional Features. The
    # operations that use them are out of scope for this contract. The values stay
    # so the contract and the Week 1 ERD describe the same domain.
    OrderStatus:
      type: string
      enum: [pending, paid, processing, shipped, delivered, cancelled]
    Role:
      type: string
      enum: [MANAGER, CLIENT, DELIVERY_PERSON]

  responses:

    # Every non-2xx response $refs one of these. DECISIONS.md item 7.
    BadRequest:
      description: The request body failed validation. `errors` names each rejected field.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Unauthorized:
      description: No token, an expired token, or a refresh row that no longer exists.
      headers:
        WWW-Authenticate:
          schema:
            type: string
          description: Required on every 401.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Forbidden:
      description: Authenticated, and not allowed to perform this action. The endpoint
        is forbidden whichever resource is named, so refusing it leaks nothing.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    NotFound:
      description: No such resource, or one this caller may not know exists. Answering
        403 here would confirm the row exists, so ownership failures return 404.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Conflict:
      description: The request conflicts with the current state. The same request can
        succeed later.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    UnprocessableEntity:
      description: Well formed, and invalid whatever the state. The same request cannot
        succeed later.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    TooManyRequests:
      description: Rate limit reached.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    InternalServerError:
      description: The server failed. The caller may retry the same request.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
